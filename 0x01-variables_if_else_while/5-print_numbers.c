#include <stdio.h>
/**
 * main -  This a C program that prints the last digit.
 *
 * Return: Always 0.
 */
int main(void)
{
int n;
for (n = 0; n <= 9; ++n)
{
printf("%d", n);
}
printf('\n');
return (0);

}
