#include "holberton.h"
/**
 *_memset - that compares two strings and returns
 *@s: variable
 *@b: variable
 *@n: variable
 *Return: zero or one
 */

char *_memset(char *s, char b, unsigned int n)
{
	;
}
