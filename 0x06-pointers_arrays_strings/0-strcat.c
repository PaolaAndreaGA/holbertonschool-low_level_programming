#include "holberton.h"
#include <stdio.h>

/**
 *_strcat - function that concatenates two strings.
 *@dest: string
 *@src: string
 * Return: string
 */
char *_strcat(char *dest, char *src)
{


	while(*src)
	{
		src++;
		
	}
	while(src++ == dest++)
	{
		;
		
	}
	return (dest);
}
