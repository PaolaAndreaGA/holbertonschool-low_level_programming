#include "holberton.h"
#include <stdio.h>

/**
 * rev_string - function that reverses a string.
 *@s: variable
 * Return: Always 0.
 */

void rev_string(char *s)
{

	int c = 0;
	int k;

	while (s[c] != '\0')
	{
		c++;
	}

	for (k = c - 1; k >= 0; k--)
	{

	}

}
